<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/modernizr-2.6.2.js"></script>
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
@*because identity is registered, all classes in the app(pages) inherit from it, getting all its attibutes like User, check with debbuger *@
                    @if (@User.Identity.IsAuthenticated)
                    {
                        @Html.ActionLink("Hola  " + @User.Identity.Name + "!", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                    }
                    else
                    {
                        @Html.ActionLink("Login", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                    }

            </div>

@if (@User.Identity.IsAuthenticated)
{

                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav navbar-right">

                        <li>
                            <a href="@Url.Action("Index", "Report")">Reportes</a>
                        </li>

                        <li>
                            <a href="@Url.Action("Index", "User")">Usuarios</a>
                        </li>

                        <li>
                            <a href="@Url.Action("Index", "Project")">Proyectos</a>
                        </li>
                        <li>
                            <a href="@Url.Action("Index", "ProjectTask")">Tareas</a>
                        </li>
                      
                        <li>
                            <a href="@Url.Action("Logout", "Auth")">Logout</a>
                        </li>
   
                    </ul>
            </div>
}

        </div>
    </div>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - TCU-Project ASP.NET MVC </p>
        </footer>
    </div>

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
</body>
</html>